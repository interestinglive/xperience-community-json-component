namespace XperienceCommunity.JsonComponent.Enum;

/// <summary>
/// An enum which indicates the input type used for editing a property value in the administration.
/// </summary>
public enum JsonInputType
{
    Text,
    Number,
    Dropdown,
    Checkbox,
    RadioGroup,
    MultipleChoice
}
